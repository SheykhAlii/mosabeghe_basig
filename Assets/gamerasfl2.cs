using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class gamerasfl2 : MonoBehaviour
{
    public gameobgact1 gam;
    // Start is called before the first frame update
    void Start()
    {
        gam.salam = 50;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
